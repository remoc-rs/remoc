[package]
name = "pizzeria-client"
version = "0.1.0"
edition = "2024"

[dependencies]
pizzeria = { path = "../pizzeria" }
remoc = { path = "../../../remoc" }
futures = "0.3"
tokio = { version = "1", features = ["rt-multi-thread", "net", "sync", "time"] }
tracing = "0.1"
